# vision-guard
This system is a web-based platform that receives video streams and processes them in real time. Incoming video data is analyzed using trained deep learning models to automatically detect violent or aggressive behavior against people. The system generates alerts and allows authorized users to review relevant video segments through a web interface.
